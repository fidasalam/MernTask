// utils/notifications.js
export const showNotification = (message) => {
    // This could be replaced with a custom notification library or component
    window.alert(message); // For simplicity, we use window.alert here
  };
  